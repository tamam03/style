<!--コメント投稿-->
<%= form_with model: comment, url: public_item_comments_path(item), method: :post do |f| %>

  <%= f.label :text, "コメント" %>
  <%= f.text_area :text, placeholder: "コメントをここに" %>
  <%#= f.hidden_field :item_id, value: item.id %>
  <%= f.submit "送信" %>

<% end %>


<!--item id取得をしてログインユーザーのidで投稿-->
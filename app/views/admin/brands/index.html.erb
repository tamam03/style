<h1>ブランド一覧</h1>

<%= form_with model: @brand, url: admin_brands_path, method: :post do |f| %>

  <%= f.text_field :brand_name, placeholder: "ブランド名" %>

  <%= f.submit "登録" %>
<% end %>



<p>ブランドリスト</p>
<% @brands.each do |brand| %>
   <%= brand.brand_name %><br>
<% end %>


